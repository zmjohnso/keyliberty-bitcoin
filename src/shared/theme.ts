import { createTheme } from "@mui/material";

export const theme = () =>
  createTheme({
    palette: {
      primary: {
        main: "#12B9F7",
      },
      secondary: {
        main: "#F74F12",
      },
    },
  });
